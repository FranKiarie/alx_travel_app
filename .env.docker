# ---- Django ----
DEBUG=True
# Generate: python -c "from django.core.management.utils import get_random_secret_key as g; print(g())"
SECRET_KEY=REPLACE_WITH_A_REAL_SECRET
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Allow a local frontend (optional)
# CORS_ALLOWED_ORIGINS=http://localhost:3000

# ---- Database (MySQL in Docker) ----
DB_ENGINE=mysql
DB_NAME=alx_travel_db
DB_USER=alx_user
DB_PASSWORD=strongpass
DB_HOST=db
DB_PORT=3306

# ---- Celery (optional) ----
# CELERY_BROKER_URL=amqp://guest:guest@rabbit:5672//
# CELERY_RESULT_BACKEND=rpc://

# MySQL root password for healthcheck (env for db service)
MYSQL_ROOT_PASSWORD=rootpass
